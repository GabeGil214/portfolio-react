import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles({
  container: {
    flexGrow: 1,
    height: '90vh',
    overflowX: 'hidden',
  },
  space: {
    padding: '0 15px',
  }

})

export default useStyles
